import {chromium} from '/Users/poli/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs';import assert from 'node:assert/strict';
const b=await chromium.launch({headless:true,executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'}),p=await b.newPage({viewport:{width:390,height:844},isMobile:true,hasTouch:true}),errors=[];p.on('pageerror',e=>errors.push(e.message));
try{await p.route('**/finale.js',async r=>{const response=await r.fetch();await r.fulfill({response,body:await response.text()+`\nwindow.__bend=()=>{s=newRide();startRide(s);Object.assign(s.cart,{c:1,r:2,x:120,y:130,dir:'up',to:{c:1,r:1},progress:70});s.queued='up';};`});});await p.goto('http://localhost:8089/two-d/level-11.html');await p.waitForFunction(()=>frightFinale?.running);await p.evaluate(()=>__bend());await p.waitForFunction(()=>frightFinale.cart.dir==='right');await p.waitForTimeout(400);const s=await p.evaluate(()=>frightFinale);assert.ok(s.cart.x>140);assert.equal(s.phase,'ride');assert.equal(s.queued,'right');assert.deepEqual(errors,[]);console.log('Live mobile cart passes the corner without steering or stopping.');}finally{await b.close();}
