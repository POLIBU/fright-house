import {chromium} from '/Users/poli/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs';import assert from 'node:assert/strict';
const b=await chromium.launch({headless:true,executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'});try{const p=await b.newPage({viewport:{width:1200,height:1000}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://localhost:8089/two-d/level-11.html?scene=ending');await p.getByRole('button',{name:'WATCH ENDING',exact:true}).click();for(const [age,name]of [[7.8,'clown-shadow'],[11.5,'circle-zoom'],[14.6,'the-end']]){await p.waitForFunction(t=>frightFinale.age>=t,age);await p.locator('#game').screenshot({path:`validation/finale-v35/${name}.png`});const state=await p.evaluate(()=>frightFinale);if(name==='clown-shadow')assert.ok(state.endingCamera.shadowVisible);if(name==='circle-zoom'){assert.ok(state.endingCamera.zoom>1.5&&state.endingCamera.iris>0);assert.ok(state.audio.sunriseMusic);}if(name==='the-end')assert.equal(state.endingCamera.title,1);}await p.waitForFunction(()=>frightFinale.phase==='complete');assert.ok(await p.locator('#veil').evaluate(e=>e.classList.contains('ending-menu')));await p.getByRole('button',{name:'WATCH AGAIN',exact:true}).click();assert.equal(await p.evaluate(()=>frightFinale.phase),'escape');assert.equal(await p.evaluate(()=>frightFinale.audio.sunriseMusic),false);assert.deepEqual(errors,[]);console.log('Clown shadow, camera zoom, iris close, styled ending title, music and replay pass.');}finally{await b.close();}
