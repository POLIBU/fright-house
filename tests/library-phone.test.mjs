import test from 'node:test';import assert from 'node:assert/strict';import * as THREE from '../vendor/three.module.js';import generate from '../two-d/assets/three/library-phone.js';
test('receiver cord remains attached throughout lifting and lowering',()=>{const asset=generate(THREE),receiver=asset.getObjectByName('receiver'),cord=asset.getObjectByName('stretching-cord');let restEnd;for(const lift of [0,.25,.5,.75,1,.5,0]){asset.userData.setReceiver(lift);const p=cord.geometry.attributes.position;for(const [i,expected]of [[0,asset.userData.cordEndpoints.body],[180,asset.userData.cordEndpoints.receiver]]){const center=new THREE.Vector3();for(let j=0;j<6;j++)center.add(new THREE.Vector3().fromBufferAttribute(p,i*7+j));center.multiplyScalar(1/6);assert.ok(center.distanceTo(new THREE.Vector3(...expected))<1e-6);}const end=new THREE.Vector3(-.192,-.007,0).applyMatrix4(receiver.matrix);assert.ok(end.distanceTo(new THREE.Vector3(...asset.userData.cordEndpoints.receiver))<1e-9);if(lift===0)restEnd=end;if(lift===1)assert.ok(end.y-restEnd.y>.2);assert.ok([...p.array].every(Number.isFinite));}});
