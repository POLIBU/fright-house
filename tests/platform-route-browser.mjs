import {chromium} from '/Users/poli/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs';
import assert from 'node:assert/strict';import fs from 'node:fs/promises';
const browser=await chromium.launch({headless:true,executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome',args:['--no-sandbox']});
try{const page=await browser.newPage({viewport:{width:1200,height:1060}}),errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('http://localhost:8089/two-d/level-3.html');await page.locator('#start:not([disabled])').click();let oldFloor=-1,lastJump=-2,attempts=0,finish=null,held=null;const floors=[];const started=Date.now();while(Date.now()-started<100000){let s=await page.evaluate(()=>window.frightPlatform);if(s.status==='escaped'){finish=s;break;}if(s.status==='caught'){attempts++;await page.locator('#retry').click();held=null;lastJump=-2;continue;}if(s.floor!==oldFloor){oldFloor=s.floor;floors.push(s.floor);await page.screenshot({path:`validation/platform/route-floor-${s.floor+1}.png`});}
const fuseX=[450,410,480][s.floor],needFuse=s.floor<3&&!s.fuses.includes(s.floor);let dir=needFuse?(s.x<fuseX?'ArrowRight':'ArrowLeft'):(s.floor===0||s.floor===2?'ArrowRight':'ArrowLeft');const atStairs=s.floor===0?s.x>852:s.floor===1?s.x<86:s.floor===2?s.x>875:s.x<166;
if(needFuse&&Math.abs(s.x-fuseX)<12){if(held)await page.keyboard.up(held);held=null;}else if((atStairs&&!needFuse)||s.stair){if(held)await page.keyboard.up(held);held=null;if(!s.stair)await page.keyboard.press('e');}
else {if(held!==dir){if(held)await page.keyboard.up(held);await page.keyboard.down(dir);held=dir;}if(s.time-lastJump>.82&&(!needFuse||Math.abs(s.x-fuseX)>60)){await page.keyboard.press('Space');lastJump=s.time;}}
if(s.floor===0&&!s.power&&Math.abs(s.x-360)<24)await page.keyboard.press('e');if(s.floor===3&&s.cooldown===0)await page.locator('#flash').click();await page.waitForTimeout(55);}
const final=await page.evaluate(()=>window.frightPlatform);await page.screenshot({path:'validation/platform/route-end.png'});await fs.writeFile('validation/platform/route-results.json',JSON.stringify({floors,attempts,seconds:(Date.now()-started)/1000,errors,final},null,2));assert.ok(finish,JSON.stringify(final));assert.deepEqual(errors,[]);assert.equal(finish.fuses.length,3);console.log(JSON.stringify({floors,attempts,seconds:(Date.now()-started)/1000,health:finish.health,hits:finish.hits,errors}));}finally{await browser.close();}
