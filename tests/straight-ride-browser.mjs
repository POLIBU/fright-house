import {chromium} from '/Users/poli/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs';import assert from 'node:assert/strict';import {routeSteer,routeJump} from './finale-driver.mjs';
const b=await chromium.launch({headless:true,executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'});
try{const p=await b.newPage({viewport:{width:1100,height:900}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://localhost:8089/two-d/level-11.html');await p.waitForFunction(()=>window.frightFinale?.running);await p.locator('header span').click();let held=null,shot=false;const deadline=Date.now()+150000;
while(!p.url().includes('level-12')&&Date.now()<deadline){const s=await p.evaluate(()=>window.frightFinale).catch(e=>{if(/Execution context was destroyed/.test(e.message))return null;throw e;});if(!s){await p.waitForTimeout(50);continue;}assert.notEqual(s.phase,'caught');assert.equal(s.cart.dir,'up');assert.ok(s.render.drawCalls<900&&s.render.triangles<1500000);const next=routeSteer(s);if(next!==held){if(held)await p.keyboard.up(held==='left'?'ArrowLeft':'ArrowRight');if(next)await p.keyboard.down(next==='left'?'ArrowLeft':'ArrowRight');held=next;}if(routeJump(s)&&s.jumpCooldown===0)await p.keyboard.press('Space');if(!shot&&s.cart.distance>1400){shot=true;await p.locator('#game').screenshot({path:'/tmp/fright-straight-ride.png'});console.log('First hazard section rendered; peak scene budget is within limits.');}await p.waitForTimeout(35);}
await p.waitForURL('**/level-12.html?arrival=cart',{timeout:5000});assert.deepEqual(errors,[]);console.log('Full forward ride completed with real steering/jump keys; exits into the alley.');
}finally{await b.close();}
