import {chromium} from '/Users/poli/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs';import assert from 'node:assert/strict';
const b=await chromium.launch({headless:true,executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'});try{for(const mobile of [false,true]){const p=await b.newPage({viewport:mobile?{width:430,height:932}:{width:1200,height:1000},isMobile:mobile,hasTouch:mobile}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://localhost:8089/two-d/index.html');assert.equal(await p.locator('#title-splash').count(),0);assert.equal(await p.locator('main').evaluate(e=>e.inert),false);await p.waitForFunction(()=>document.querySelector('#veil h1 img')?.naturalWidth>0);await p.screenshot({path:`validation/finale-v35/${mobile?'touch':'desktop'}-direct-intro.png`});await p.locator('#start:not([disabled])').click();assert.equal(await p.locator('#veil').isVisible(),false);assert.deepEqual(errors,[]);await p.close();}console.log('Direct Level 1 intro, transparent heading logo and desktop/touch start pass.');}finally{await b.close();}
