import {chromium} from '/Users/poli/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs';import assert from 'node:assert/strict';
const browser=await chromium.launch({headless:true,executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'}),p=await browser.newPage({viewport:{width:390,height:844},isMobile:true,hasTouch:true,reducedMotion:'reduce'}),errors=[];p.on('pageerror',e=>errors.push(e.message));
try{await p.route('**/toystore.js',async r=>{const res=await r.fetch();await r.fulfill({response:res,body:await res.text()+`\nwindow.__prepare=()=>{s=retryStore();s.invincible=999;storeAction(s,'music');inspector.sync();};`});});await p.goto('http://localhost:8089/two-d/level-4.html');await p.waitForFunction(()=>frightStore?.running);await p.evaluate(()=>__prepare());await p.locator('#wind-key').tap();await p.waitForFunction(()=>frightStore.drawerOpen===1);await p.locator('#rabbit-collect').tap();await p.locator('#instructions-menu[open]').waitFor();await p.locator('#instructions-menu button').tap();await p.waitForFunction(()=>frightStore.phase==='dimming'&&!frightStore.paused);assert.equal(await p.locator('#story-overlay').isVisible(),false);await p.waitForFunction(()=>frightStore.phase==='blackout');assert.equal(await p.locator('#story-overlay').isVisible(),false);await p.waitForFunction(()=>frightStore.phase==='chase');await p.waitForTimeout(600);assert.equal(await p.locator('#story-overlay').isVisible(),false);assert.deepEqual(errors,[]);console.log('Mobile rabbit collection still opens instructions; dimming, blackout and active toy chase have no automatic dialogue overlay.');}finally{await browser.close();}
