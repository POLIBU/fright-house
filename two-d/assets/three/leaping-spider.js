export default function generate(THREE){const g=new THREE.Group(),shell=new THREE.MeshStandardMaterial({color:0x463b30,roughness:.87}),joint=new THREE.MeshStandardMaterial({color:0x896847,roughness:.8}),eye=new THREE.MeshStandardMaterial({color:0xba3b22,emissive:0x6b160a,emissiveIntensity:.7});
function ell(x,y,z,sx,sy,sz,mat){const m=new THREE.Mesh(new THREE.SphereGeometry(1,10,6),mat);m.position.set(x,y,z);m.scale.set(sx,sy,sz);g.add(m);return m;}ell(-.06,.13,0,.14,.1,.105,shell);ell(.095,.12,0,.078,.065,.075,shell);for(const z of [-.037,.037])ell(.16,.14,z,.014,.012,.01,eye);
const legs=[];for(const side of [-1,1])for(let i=0;i<4;i++){const leg=new THREE.Group();leg.name='leg-'+side+'-'+i;leg.position.set((i-1.5)*.065,.12,side*.055);g.add(leg);function rod(a,b){const p=new THREE.Vector3(...a),q=new THREE.Vector3(...b),delta=q.clone().sub(p),m=new THREE.Mesh(new THREE.CylinderGeometry(.007,.012,delta.length(),5),joint);m.position.copy(p.add(q).multiplyScalar(.5));m.quaternion.setFromUnitVectors(new THREE.Vector3(0,1,0),delta.normalize());leg.add(m);}const reach=(i-1.5)*.055;rod([0,0,0],[reach,.035,side*.14]);rod([reach,.035,side*.14],[reach*1.6,-.113,side*.23]);legs.push(leg);}g.userData.legs=legs;return g;}
