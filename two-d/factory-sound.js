export function createFactorySound(){let ac,gain,muted=false,started=false,lastClank=-1,lastSteam=-1;function start(){ac||=new AudioContext();ac.resume();if(started)return;started=true;gain=ac.createGain();gain.gain.value=muted?0:.10;gain.connect(ac.destination);for(const f of [47,71]){const o=ac.createOscillator(),g=ac.createGain();o.type='triangle';o.frequency.value=f;g.gain.value=.16;o.connect(g).connect(gain);o.start();}}function tone(f,d,v){if(!started)return;const o=ac.createOscillator(),g=ac.createGain();o.type='triangle';o.frequency.setValueAtTime(f,ac.currentTime);o.frequency.exponentialRampToValueAtTime(f*.55,ac.currentTime+d);g.gain.setValueAtTime(v,ac.currentTime);g.gain.exponentialRampToValueAtTime(.001,ac.currentTime+d);o.connect(g).connect(gain);o.start();o.stop(ac.currentTime+d);}function hiss(){if(!started)return;const buffer=ac.createBuffer(1,ac.sampleRate*.65,ac.sampleRate),data=buffer.getChannelData(0);for(let i=0;i<data.length;i++)data[i]=(Math.random()*2-1)*(1-i/data.length);const source=ac.createBufferSource(),filter=ac.createBiquadFilter(),g=ac.createGain();source.buffer=buffer;filter.type='bandpass';filter.frequency.value=1600;g.gain.value=.35;source.connect(filter).connect(g).connect(gain);source.start();}return {start,tick(s){if(!started||s.paused||s.inspection)return;const c=Math.floor(s.time*2.3);if(c!==lastClank){lastClank=c;tone(c%3?140:85,.15,.16);}const steam=Math.floor((s.time+.8)/2.4);if(steam!==lastSteam){lastSteam=steam;hiss();}},pause:()=>ac?.suspend(),resume:()=>ac?.resume(),mute(v){muted=v;if(gain)gain.gain.value=v?0:.10;},reset(){lastClank=-1;lastSteam=-1;},click:()=>tone(230,.16,.35),snapshot:()=>({started,muted,kind:'factory motor, gear clank and steam'})};}
