export function newCarousel(opened=false){return {phase:opened?'open':'idle',age:0,angle:0,keyAngle:0};}
export function windCarousel(s){if(s.phase!=='idle')return false;s.phase='winding';s.age=0;return true;}
export function tickCarousel(s,dt){dt=Math.max(0,Math.min(.05,dt));if(['idle','open'].includes(s.phase))return null;s.age+=dt;if(s.phase==='winding'){s.keyAngle-=dt*9;if(s.age>=.7){s.phase='music';s.age=0;return 'music';}}else if(s.phase==='music'){s.angle+=dt*(1.3+s.age*.1);if(s.age>=3.8){s.phase='opening';s.age=0;return 'opening';}}else if(s.phase==='opening'&&s.age>=1.2){s.phase='open';s.age=0;return 'open';}return null;}
export function poseCarousel(root,s){const {lid,carousel,key,fragments}=root.userData.joints;key.rotation.x=s.keyAngle;carousel.rotation.y=s.angle;lid.rotation.x=-1.25*(s.phase==='open'?1:s.phase==='opening'?Math.min(1,s.age/1.2):0);for(const f of fragments){const b=f.userData.burst;f.position.copy(b.position);f.rotation.copy(b.rotation);}}
