// Approved revised investigator: four directions; source sheet is preserved unchanged.
const sheet=new Image(),frames=[];
sheet.onload=()=>{const c=document.createElement('canvas');c.width=sheet.width;c.height=sheet.height;const cx=c.getContext('2d',{willReadFrequently:true});cx.drawImage(sheet,0,0);const pixels=cx.getImageData(0,0,c.width,c.height).data;for(let frame=0;frame<4;frame++){const left=Math.floor(frame*c.width/4),right=Math.floor((frame+1)*c.width/4);let x0=right,y0=c.height,x1=left,y1=0;for(let y=0;y<c.height;y++)for(let x=left;x<right;x++)if(pixels[(y*c.width+x)*4+3]>90){x0=Math.min(x0,x);x1=Math.max(x1,x);y0=Math.min(y0,y);y1=Math.max(y1,y);}frames.push({x:x0,y:y0,w:x1-x0+1,h:y1-y0+1});}};
sheet.src=new URL('../assets/characters/investigator-v2.png',import.meta.url).href;
function sprite(ctx,s,moving){if(frames.length!==4)return false;const f=frames[{down:0,left:1,up:2,right:3}[s.face]??0],h=31,w=h*f.w/f.h,step=moving?Math.sin(s.walk)*.8:0,x=Math.round(s.x-w/2),y=Math.round(s.y-h),cut=Math.floor(f.h*.8);ctx.imageSmoothingEnabled=false;ellipse(ctx,s.x,s.y+1,7,2,'#09080b99');ctx.drawImage(sheet,f.x,f.y,f.w,cut,x,y-Math.abs(step)*.4,w,h*.8);for(let side=0;side<2;side++)ctx.drawImage(sheet,f.x+side*f.w/2,f.y+cut,f.w/2,f.h-cut,x+side*w/2,y+h*.8+(side?step:-step),w/2,h*.2);return true;}
function rect(ctx,x,y,w,h,c){ctx.fillStyle=c;ctx.fillRect(Math.round(x),Math.round(y),w,h);}
function ellipse(ctx,x,y,rx,ry,c){ctx.fillStyle=c;ctx.beginPath();ctx.ellipse(x,y,rx,ry,0,0,Math.PI*2);ctx.fill();}
export function drawPlayer(ctx,s,moving){if(sprite(ctx,s,moving))return;const x=Math.round(s.x),y=Math.round(s.y),bob=moving?Math.sin(s.walk)*.7:0,feet=moving?Math.sin(s.walk)*2:0;ellipse(ctx,x,y+1,8,3,'#09080bb0');rect(ctx,x-5,y-7,4,7+feet,'#242e37');rect(ctx,x+1,y-7,4,7-feet,'#293641');rect(ctx,x-6,y-13+bob,12,9,'#372429');rect(ctx,x-5,y-14+bob,10,8,'#775b40');rect(ctx,x-5,y-14+bob,2,7,'#a27b51');rect(ctx,x-4,y-23+bob,9,9,'#372524');rect(ctx,x-3,y-22+bob,7,7,s.face==='up'?'#42312b':'#c5976a');rect(ctx,x-5,y-23+bob,10,4,'#302326');rect(ctx,x-6,y-21+bob,3,5,'#302326');if(s.face!=='up'){rect(ctx,x+(s.face==='left'?-3:2),y-19+bob,1,2,'#171a21');rect(ctx,x,y-15+bob,4,1,'#8e573f');}const dir=s.face==='left'?-1:1;rect(ctx,x+dir*6-2,y-12+bob,4,5,'#8a6846');rect(ctx,x+dir*8-2,y-10+bob,5,3,'#252a2e');rect(ctx,x+dir*10,y-10+bob,2,3,s.light?'#f8dea2':'#74634e');}
