import * as THREE from '../vendor/three.module.js';
import generate from './assets/three/wall-power-lever.js';
export function createPowerInspection({getState,answer,pull,close}){const dialog=document.createElement('dialog');dialog.id='power-inspection';dialog.setAttribute('aria-labelledby','power-title');dialog.innerHTML=`<button id="close-power" aria-label="Close wall inspection">CLOSE ×</button><p class="eyebrow">MAINS CABINET · SAFETY INTERLOCK</p><h2 id="power-title">The face on the wall</h2><div class="power-layout"><section class="riddle-paper"><p class="engraving">“I have hands, but cannot clap.<br>I have a face, but cannot smile.<br>I tell you when the fun must end.<br>What am I?”</p><p>Press the matching brass key.</p><div class="riddle-answers"><button data-answer="mirror">MIRROR</button><button data-answer="clock">CLOCK</button><button data-answer="doll">DOLL</button></div><p id="power-message" aria-live="polite"></p></section><section class="lever-panel"><span id="lever-lamp">INTERLOCK ENGAGED</span><canvas id="lever-view" width="280" height="310" aria-label="Three-dimensional red mains lever. Select the correct riddle answer, then click the lever to pull it."></canvas><button id="pull-lever" disabled>PULL RED LEVER</button><small>Click the red handle or use the button.</small></section></div>`;document.body.append(dialog);
let renderer,scene,camera,asset;function init(){if(renderer)return;renderer=new THREE.WebGLRenderer({canvas:dialog.querySelector('canvas'),alpha:true,antialias:true});renderer.setSize(280,310,false);renderer.setPixelRatio(Math.min(devicePixelRatio,1.5));renderer.outputColorSpace=THREE.SRGBColorSpace;scene=new THREE.Scene();camera=new THREE.PerspectiveCamera(35,280/310,.01,10);camera.position.set(.7,.59,1.65);camera.lookAt(0,.36,0);asset=generate(THREE);scene.add(asset,new THREE.HemisphereLight(0xe4d9b8,0x1b211e,2));const lamp=new THREE.DirectionalLight(0xffc78b,3);lamp.position.set(-1,2,3);scene.add(lamp);}
dialog.querySelector('#close-power').onclick=close;dialog.addEventListener('cancel',e=>{e.preventDefault();close();});for(const b of dialog.querySelectorAll('[data-answer]'))b.onclick=()=>answer(b.dataset.answer);dialog.querySelector('#pull-lever').onclick=pull;dialog.querySelector('canvas').onclick=pull;
return {sync(){const s=getState();if(s.inspection==='power'){init();if(!dialog.open){dialog.showModal();dialog.querySelector('[data-answer]').focus();}dialog.querySelector('#power-message').textContent=s.puzzleMessage;dialog.querySelector('#lever-lamp').textContent=s.power?'MAINS LIVE':s.riddleSolved?'INTERLOCK RELEASED':'INTERLOCK ENGAGED';dialog.querySelector('#lever-lamp').classList.toggle('live',s.riddleSolved);for(const b of dialog.querySelectorAll('[data-answer]')){b.disabled=s.riddleSolved||s.paused;b.setAttribute('aria-pressed',String(s.riddleSolved&&b.dataset.answer==='clock'));}const b=dialog.querySelector('#pull-lever');b.disabled=!s.riddleSolved||s.power||s.pulling||s.paused;b.textContent=s.power?'POWER ON':s.pulling?'PULLING…':'PULL RED LEVER';asset.userData.lever.rotation.x=.2+s.lever*2.4;renderer.render(scene,camera);}else if(dialog.open)dialog.close();},snapshot(){return {open:dialog.open,angle:asset?.userData.lever.rotation.x,triangles:renderer?.info.render.triangles};}};}
