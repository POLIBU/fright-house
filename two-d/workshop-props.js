import makeExitDoor from './assets/three/workshop-exit.js';
import {workshopReveal} from './levels/workshop-model.js';
import * as THREE from '../vendor/three.module.js';import platform from './assets/three/workshop-platform.js';
export function createWorkshopProps(background){const renderer=new THREE.WebGLRenderer({alpha:true,antialias:false});renderer.setSize(480,360,false);renderer.outputColorSpace=THREE.SRGBColorSpace;const scene=new THREE.Scene(),camera=new THREE.OrthographicCamera(0,480,360,0,.1,2000);camera.position.z=1000;scene.add(new THREE.HemisphereLight(0xb9c4b6,0x151c1c,1.7));const key=new THREE.DirectionalLight(0xffd69a,2.2);key.position.set(-100,420,700);scene.add(key);const group=new THREE.Group(),pose=new THREE.Group(),asset=platform(THREE);pose.add(asset);pose.rotation.x=.82;group.add(pose);pose.updateMatrixWorld(true);const box=new THREE.Box3().setFromObject(pose),size=box.getSize(new THREE.Vector3()),centre=box.getCenter(new THREE.Vector3());pose.scale.set(185/size.x,86/size.y,86/size.y);pose.position.set(-centre.x*pose.scale.x,-box.min.y*pose.scale.y,0);group.position.set(360,360-333,0);scene.add(group);const exitDoor=makeExitDoor(THREE);if(background){const paint=document.createElement('canvas');paint.width=70;paint.height=98;const c=paint.getContext('2d');c.imageSmoothingEnabled=false;c.drawImage(background,622/720*background.width,28/540*background.height,35/720*background.width,49/540*background.height,0,0,70,98);const texture=new THREE.CanvasTexture(paint);texture.colorSpace=THREE.SRGBColorSpace;texture.magFilter=THREE.NearestFilter;const original=exitDoor.userData.leaf.material;exitDoor.userData.leaf.material=[original,original,original,original,new THREE.MeshBasicMaterial({map:texture}),original];exitDoor.userData.star.visible=false;}exitDoor.scale.set(35/.88,49/1.3,23);exitDoor.position.set(622+exitDoor.userData.originShift*(35/.88),360-77,0);scene.add(exitDoor);return {draw(ctx,s){const reveal=workshopReveal(s);exitDoor.userData.hinge.rotation.y=-s.exit*1.45;group.position.y=27-reveal.offset;camera.position.x=s.cameraX;camera.position.y=-s.cameraY;camera.updateMatrixWorld();if(reveal.lift<1){group.visible=false;renderer.render(scene,camera);ctx.drawImage(renderer.domElement,0,0);group.visible=true;exitDoor.visible=false;}renderer.render(scene,camera);exitDoor.visible=true;ctx.save();if(reveal.lift<1){ctx.beginPath();ctx.rect(260-s.cameraX,249-s.cameraY,200,92);ctx.clip();}ctx.drawImage(renderer.domElement,0,0);ctx.restore();},snapshot(){return {platform3D:true,hatchMaterial:'steel',exitDoor3D:true,triangles:renderer.info.render.triangles,calls:renderer.info.render.calls};}};}
